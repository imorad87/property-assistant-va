import { Grid } from '@mui/material'
import React from 'react'

const SendMessageForm = ({ message, number, isScheduled, submitHandler }) => {
    return (
        <Grid container>
            
        </Grid>
    )
}

export default SendMessageForm