export enum Constants {
    // message specific 
    FAILED = 'failed',
    SENT = 'sent',
    OUTGOING = 'outgoing',
    INCOMING = 'incoming',
    RECEIVED = 'received',
    SCHEDULED = 'scheduled',

    // files specific
    PARSING = 'parsing',
    COMPLETED= 'completed',


    // phone number specific
    PAUSED = 'paused',
    ACTIVE = 'active',

    // lead specific
    LEAD = 'lead',
    CONVERTED = 'converted'
}